import { render, screen } from '@testing-library/react';
import { TodoCardList } from './todo-card-list';

describe('TodoCardList', () => {
  it('Should render successfully', () => {
    // Steps:
    /*
      Check if the component renderers
    */
  });
});
