import { render, screen } from '@testing-library/react';
import { Search } from './search';

describe('Search', () => {
  it('Should send fake request with searched data', () => {
    // Steps:
    /*
      Arrange
      1. render Search.
      2. save fake fetch function
      3. reference document input 

      Act
      4. enter any input with user.type from user-event react testing library

      Assert
      5. expect written data to be sen.
    */
  });
});
